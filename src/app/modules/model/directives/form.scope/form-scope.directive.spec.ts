import { FormScopeDirective } from './form-scope.directive';

describe('FormScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormScopeDirective();
    expect(directive).toBeTruthy();
  });
});
