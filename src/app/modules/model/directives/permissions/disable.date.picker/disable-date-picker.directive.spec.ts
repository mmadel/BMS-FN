import { DisableDatePickerDirective } from './disable-date-picker.directive';

describe('DisableDatePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDatePickerDirective();
    expect(directive).toBeTruthy();
  });
});
