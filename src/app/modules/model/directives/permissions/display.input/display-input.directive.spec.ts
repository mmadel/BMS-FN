import { DisplayInputDirective } from './display-input.directive';

describe('DisplayInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayInputDirective();
    expect(directive).toBeTruthy();
  });
});
