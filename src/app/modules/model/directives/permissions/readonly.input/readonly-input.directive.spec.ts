import { ReadonlyInputDirective } from './readonly-input.directive';

describe('ReadonlyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadonlyInputDirective();
    expect(directive).toBeTruthy();
  });
});
