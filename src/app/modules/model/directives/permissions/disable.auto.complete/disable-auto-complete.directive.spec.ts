import { DisableAutoCompleteDirective } from './disable-auto-complete.directive';

describe('DisableAutoCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableAutoCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
