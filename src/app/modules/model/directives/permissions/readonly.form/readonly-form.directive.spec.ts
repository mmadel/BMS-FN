import { ReadonlyFormDirective } from './readonly-form.directive';

describe('ReadonlyFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadonlyFormDirective();
    expect(directive).toBeTruthy();
  });
});
