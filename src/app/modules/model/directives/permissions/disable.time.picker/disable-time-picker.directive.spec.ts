import { DisableTimePickerDirective } from './disable-time-picker.directive';

describe('DisableTimePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableTimePickerDirective();
    expect(directive).toBeTruthy();
  });
});
