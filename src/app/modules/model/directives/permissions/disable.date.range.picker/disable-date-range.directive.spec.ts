import { DisableDateRangeDirective } from './disable-date-range.directive';

describe('DisableDateRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDateRangeDirective();
    expect(directive).toBeTruthy();
  });
});
