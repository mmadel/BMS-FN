import { ScopeDirective } from './scope.directive';

describe('ScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScopeDirective();
    expect(directive).toBeTruthy();
  });
});
